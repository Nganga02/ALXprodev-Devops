## Understanding bash scripting in more details
By leveraging the following commands used in the linux shell i was able to perform a little bit of api data retriaval
1. `awk`
2. `sed`
3. `curl`
4. `tr`

The api being used was the [Pokemon API](https://pokeapi.co/api/v2/pokemon/) the folder Advanded shell highlights the tasks that I was able to accomplish using the above commands